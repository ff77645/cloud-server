{
  "name": "cloud-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "cross-env NODE_ENV=development wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "hono": "^3.10.3"
  }
}
